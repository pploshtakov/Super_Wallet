<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:tools="http://schemas.android.com/tools"
      android:id="@+id/category_list_layout"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:paddingBottom="@dimen/activity_vertical_margin"
      android:paddingLeft="@dimen/activity_horizontal_margin"
      android:paddingRight="@dimen/activity_horizontal_margin"
      android:paddingTop="@dimen/activity_vertical_margin"
      tools:context="com.example.pesho.superwallet.CategoryListActivity"
      android:orientation="vertical"
    >

    <Button
        android:id="@+id/add_account_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/add_new_account"
        />

    <GridView
        android:id="@+id/accounts_grid_view"
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:horizontalSpacing="10dip"
        android:numColumns="4"
        android:verticalSpacing="10dip" />

    <ImageView
        android:id="@+id/accounts_trash_can"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:contentDescription="@string/delete_string"
        android:padding="40dip"
        android:src="@drawable/trash"
        android:visibility="gone" >
    </ImageView>


</LinearLayout>
